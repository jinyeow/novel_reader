defmodule NovelReader.NovelUpdates.ChapterUpdate do
  defstruct [:chapter_url, :pubdate, :series_url, :tags, :title, :translator]

  # TODO add functions to get/set struct fields
  # TODO add specs/types for each field
end
