defmodule NovelReader.Util.URLBuilder do

  def build_url(novel_name, chapter, site \\ "") do
    # from novel_name find corresponding site from db/ecto/ets/file
    # url = site + name + chapter
    # return url
  end
end
