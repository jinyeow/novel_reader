defmodule NovelReader.NovelUpdates.ChapterUpdateTest do
  use ExUnit.Case, async: true
  doctest NovelReader.NovelUpdates.ChapterUpdate

  # TODO add tests
  test "1 == 2" do
    assert 1 == 2, "Add tests for ChapterUpdate"
  end
end
